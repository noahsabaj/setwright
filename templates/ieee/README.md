# IEEE `IEEEtran` conference article

This template uses the `conference` option from `IEEEtran`. Author blocks and
formatting requirements vary by venue and review stage; compare the generated
paper with current official instructions before submission.

Compile with `latexmk -pdf -norc main.tex` in a trusted TeX environment, or use
Setwright's managed sandbox once that release gate is complete.
